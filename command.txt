How to use it:
Make it executable:
Run this in your terminal once:
chmod +x start_sim.sh

Run it:
./start_sim.sh

new features
./start_sim.sh (Headless/Default)

./start_sim.sh --map

./start_sim.sh --qgc

./start_sim.sh --fullmode